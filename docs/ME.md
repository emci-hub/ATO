# ME

**Name:** emci
**Twin:** Drake

## This week
**Accounts wiped for a clean retest (Aug 31, 2026).** All test accounts (riley, sam, yeezy, zintake9, lazyemci) were hard-deleted via `auth.users` cascade; `account_deletions` audit rows written for each. Only **emci2** (`lil_emci@hotmail.com`, Apple) remains — confirmed as the account to preserve. Note: its handle is `emci2`, so `require_root()`/dev-root (which key on handle `'emci'`) does **not** recognize it — a real gap to close if root actions are needed from the app. **EMCIRETEST** invite code is now owned by emci2 and set to unlimited (`max_uses NULL`, `uses_count 0`, active) — verified `assert_invite_usable('EMCIRETEST')` = true. Testers on binary 8 will see a full-screen **"Dev only."** on cold start — that is the stale embedded bundle from before commits `2fdc278`/`5f1c86b` (Aug 28) which moved all `*-lab` screens behind `__DEV__` and replaced the `Dev only.` block with a Home redirect. Binary 8 cannot receive OTA (it has no `expo-updates`). **Binary 10 is already submitted and in TestFlight** (build `1d0d1041`, submission `c0c6342d`, ASC app id `6805614731`) — testers must install it; the current production OTA `9855305a` contains the fix.

**Fixed dev-test identity (Sep 2, 2026) — the standard dev-testing method.** Real auth + `me` row `ato-dev@example.com` / `@atodev` (auth id `a70d3e0e-4c00-4a1e-8c0d-00000000d3e0`), provisioned by `supabase/migrations/wave31_dev_test_user.sql`; dev-throwaway bcrypt password in `src/lib/dev-test-user.ts` (`__DEV__` only). `__DEV__` builds auto-sign in as it on cold start when no session is cached (`use-session.ts`); real accounts are never overridden. Legends tab has a dev-only "test persona" strip (this user only) that swaps all 16 trait values between the 4 legend archetypes and clears seen-legend history (owner-delete RLS added in `wave31`). `npm run check:dev-test-user` guards it. The earlier ad-hoc `legends-dev@emgens.com` / `@legendtest` account was removed by the same migration.

**Wave 22 — Dawn categories, Explore combine, Levity, The Story (Aug 31, 2026).** Dawn Read may draw from one settled category among Steadiness / Agency / Drive; new users with none of those three settled keep knock/fact/focus exactly; Do if-then is untouched; anti-repeat and cut/crisis gates still run on the generated card. Explore may generate from any live category, at most two per entry and only when a recent signal ties them, and must not restate the pinned Categories card on the same screen. Agency-triple (GM+LC+SE) and the output fence are unchanged. 9th category **Levity** is a bar (Playfulness + conflict assertiveness + cooperativeness) — Love/closeness stays the only conflict-adjacent map. **The Story** is a separate longer-form Gemini call on its own quota (`claim_story_generate`), fingerprint-gated, thin-profile gated, no offline fallback, on the **Explore tab**. **All of that copy is unreviewed; The Story is diagnosis-adjacent — same bar as the Crisis spec. Not shippable without emci's direct read.**

**Explore is a real tab (Sep 1, 2026).** The Sage tab is clean chat only (8-ball + conversation). Explore is its own tab in the bottom bar — opened directly, no button inside Sage. It holds the axis + category summaries (`IntakeSettings` "How you show up", `TraitBandsFold` "How you tend to move", `FullProfileFold` "How you're currently leaning", and Categories at full detail via `CategoriesFold` — bars/maps + concept explainers + spotlight), "What Sage remembers" (`SageFactsCard`), The Story, the title card, Notes insight spend, and the Explore observation bubbles. "N of 16 settled" lives on the Explore tab header.

**Shared friend-voice style checklist is live across all five generation surfaces** (Dawn Read, Explore, Title, Category summaries, Story), shipped OTA `9855305a` (commit `96f61b8`): a shared six-rule style checklist + emci-approved few-shot anchors in `src/lib/voice/style-checklist.ts`. Rule 3 ("one idea at a time when possible") acknowledges Rule 6's exception (a title or category summary may name more than one quality when connected with but/yet/and, never a comma list). Also fixed the stale `check:traits` `/quieter time/` assertion.

**IA reorganization is in (15 boxes, Aug 30, 2026).** Home is card + Did/Skip + a primary slot, plus a deliberate second collapsed category teaser (Aug 31 override of the original one-slot Box 8 rule; never next to crisis/missed-check). One weekly Ask (Does-Sage-know-you / ranking / Gut call) shares the primary slot. Questions live on the Questions tab (Infinite Questions + "Want to add a bit more?" + "A faster pass"). Explore is a real bottom tab (axis grounding in `buildExplorePrompt` so tagged axis and prose agree) holding the axis + category summaries and "What Sage remembers". You holds the poster/QR, tone ("How Sage sounds"), badges, growth, support (region picker only), and Account. "This week" moved to Home. AI consent is a Dawn/Sage interstitial. Consent-off past day 3 is honest-empty Today; Did/Skip still saves (`record_check` `p_no_card`) and counts toward `check_count`. **Next: full device pass against docs/ATO_DEVICE_TESTS.md**, then Stage 8 handoff #2 (invite/referral).

**Crisis support tap is in.** An always-visible "Support" tap under the Sage composer (and at the bottom when Talk is off) opens the same static crisis card as the keyword interrupt — no message required. Keyword path, card copy, numbers, region logic, and "I'm okay, keep going" are unchanged. You's "If you need someone now" fold is the **region picker only** (not a second crisis card).

**Circle Explore is in.** Circle appears only after a QR scan / link paste connects two accounts. Category compare is **dual-opt-in** — per-friend (`category_share`) or the Close Friends pool (`close_friends_share`, on by default off). Both sides must opt in before either sees the other; `peer_category_pack` returns cached title+category summaries only, never Full Profile. Fully separate from Full Profile.

**Binary 10 in TestFlight.** Latest JS OTA: group `d5332b8b-7d5d-4898-bb5f-df121933b499` (`fb71b2d`, Legends dev-test personas). Open items: device pass, then invite/referral; verify dev-test auto-login + Legends persona strip on a real dev build; Gut Call regression; Live Talk failure; get all testers off binary 8 onto binary 10; Claude/Grok Supabase secrets if those providers will be used.

## App

- **Category:** Hybrid — AI-native (Sage/router) + Social (Circle/Chat) + Health/finance/kids (crisis spec, coaching tone)
- **Landmine modules kept:** Social (report/block required) + Health/finance/kids (privacy pass, crisis static-card required, Grok critique before code on sensitive pieces)
- **A proof (APP.md w/ non-goals):** satisfied by archive/OLD_PLAN.md.
- **B proof (3 decisions):** satisfied — platform = iOS/Expo → TestFlight → Apple; data lives in Supabase (project `ato`); accounts = yes (email OTP via Resend + Sign in with Apple on device; optional password after first login via Settings, hashed by GoTrue).
- **First 60 seconds:** open app → Home shows today's Check (read + if-then Do) with the live pixel fixed top-right, Did/Skip, then the primary slot and (when not crisis/missed-check) a collapsed category teaser — no inner tabs, no badge strip. Deliberate Aug 31, 2026 override of the original one-slot rule.
- **16-axis trait backbone (live):** openness, conscientiousness, extraversion, agreeableness, steadiness, attachment_anxiety, attachment_avoidance, conflict_assertiveness, conflict_cooperativeness, autonomy, competence, relatedness, growth_mindset, locus_of_control, self_efficacy, **playfulness** (Wave 21). Every numeric write appends `trait_history` and blends into `trait_tracks` (**told** = self-report vs **played** = gut-call; never mix; EWMA α=0.35; stability floor 3 answers; 60-day idle then 90-day half-life decay at read). Direct sources (`self_slider`, `self_tap`, `self_confirm`, `self_settings`) are sticky over inferred (`self_grid`, `self_situation`, `self_game`). Confirm-upgrade (`confirmTraitSource`) flips the source token + `last_touched` only, never the number.
- **Thin-profile rule (ACTUAL live behavior):** `THIN_PROFILE_RATIO = 6/15` applied against the **live** `TRAIT_AXES.length` (currently 16). `isThinProfile(settled, TRAIT_AXES.length)` is true when `settled / 16 < 6/15` — i.e. **fewer than 7 effective settled report-track axes of 16** (6/16 = 0.375 < 0.4 = thin; 7/16 = 0.4375 ≥ 0.4 = not thin). `settledCount` is the stability-weighted round of the effective-stability sum across all 16 report tracks; gut-call never counts. This one ratio drives Sage thin coaching, Talk depth copy, insight depth copy, Full Profile completeness (`N of 16 settled`), and the Story's thin gate. Adding a 17th axis updates it automatically.
- **Token economy:** Notes (`me.tokens` + `token_events` ledger) are earned only — check-in +3, game round +5, daily trickle +1; never 0, never purchased. Spend is a known price: extra Sage look (8 notes, no trait write) or Full Profile Depth (12 notes; real ranking pick or gut-call for that axis, 48h per-axis cooldown). Full Profile stays fully viewable regardless of balance. No paywall.
- **Full Profile (Explore tab):** collapsed Settings fold — all currently-defined axes, "not answered yet" when null, last source in plain language, last updated from `trait_touched_at`, 2-letter codes, expandable "How this has shifted" trait_history timeline, draft poles (`POLE_COPY_REVIEWED` false), AxisTaps edit with 8s undo. Completion is `N of 16 settled` (stability-weighted report-track) — invitation, never a percent of a person.
- **9 Categories (live catalog, report-track only):** Steadiness (bar), Openness to life (bar), Drive (bar), Agency (bar), Everyday social energy (bar), Communication (bar), **Levity** (bar — playfulness + conflict assertiveness + cooperativeness), Love / closeness (map, the only conflict-adjacent map), Independence & closeness (map). Bars need enough listed axes past the stability floor; maps need **both** axes independently stable. Categories render at full detail on the **Explore tab** (`CategoriesFold` — bars/maps + concept explainers + weekly spotlight, from cached title category lines). Fallback bands are banked and **unreviewed**.
- **The Story:** longer-form Gemini narrative on the Explore tab, under Categories, above the observations. Own quota lane (`claim_story_generate`), fingerprint-gated, thin-profile gated, **no offline fallback** (fold simply missing when the model is unreachable). Copy is **unreviewed, diagnosis-adjacent**.
- **Explore tab:** a real bottom tab (Home / Sage / Explore / Around / Circle / You). Holds the axis + category summaries (How you show up / How you tend to move / How you're currently leaning / Categories), "What Sage remembers", The Story, title card, Notes insight spend, and the observation bubbles with "Did this land?" reactions. The Sage tab itself is clean chat (8-ball + conversation only).
- **Voice engine:** one shared register (`sage.txt` via `VOICE_REFERENCE`), one shared style checklist + approved anchors (`STYLE_BLOCK`) applied to all five generation surfaces, plus a word-level jargon guard and phrase-pattern guard with banked fallback on generated Read/Talk/Explore.
- **Sage/Pixel relationship:** pixel is one character — current-you on Home/Around/You, aspirational-you on Sage. Same shell-mounted instance; tap moods; crisis hard-disables hands. Shape is one of 6 hashed recipes per account; color from `show_up`. You-tab poster is identity + QR.
- **Nav bar:** a custom JS 5-slot bottom bar (`react-native-sortables`) driven by a per-user `me.nav_layout` jsonb (`wave37`). **Slot 5 is always "More" (fixed, not draggable). Slots 1–4 always contain Home + Sage once each (user-draggable); the other two slots hold pool tabs from an extensible registry (Explore / You / Questions / Around / Legends / Circle).** Long-press any tab to enter "Edit navigation" (drag to reorder, add/remove pool tabs, Done to commit). Circle is a pool tab gated by `isUnlocked` (hidden until a friend is scanned) — selectable into a slot once unlocked. Default on first load: **Home / Explore / Sage / You / More**; persists across devices.
- **Floor requirements (done):** Sentry wired and JS-verified (native crash on binary 8 event `e7bed112` ingested, symbolication **unconfirmed**; org `emgens`, project `ato-app`), App Store Connect privacy labels drafted (11 types), `PrivacyInfo.xcprivacy` locked 9/9, Sage labeled "coach" except Home-in-Quest (`Sage · npc`), AI router rate-limited server-side (20/day, 200/month via `claim_ai_call()`). 16+ enforced at signup; 18+ going in Around + `set_going`.

## Roster

- Assistant (Drake) — active
- Builder (Cursor) — active. Current model: Grok 4.6. Owns docs/ME.md, docs/NOW.md, docs/archive/OLD_PLAN.md, and docs/BUSINESS.md — updates committed **and pushed** together, never left local-only.

## Live AI + model

- Cursor: Grok 4.6 (current) — crisis/safety-critical work always routes to Claude Opus 5 (fixed lane) regardless of Home/Away
- App's own router: `EXPO_PUBLIC_AI_PROVIDER` (default `gemini`). Gemini / NVIDIA / Perplexity are client-side; Claude / Grok via the `ai-generate` Edge Function. `local` is the deterministic fallback. Server-side rate-limited (20/day, 200/month per user). On-device override (5-tap Build line) is AsyncStorage, not synced. Production OTA `b84f0aa6` includes this layer. Gemini key rotated Sep 1, 2026 (`.env.local` + EAS production env; never committed).

## Unreviewed copy — one list, waiting on emci (Aug 31, 2026)

Every flag below is `false` in code; nothing here is shippable without emci's direct read. Story/Levity are diagnosis-adjacent — same bar as the Crisis spec.

1. **The Story + told-vs-played samples** — `STORY_COPY_REVIEWED = false` (`src/lib/sage-story.ts`). Sample paragraph, 3 tension lines, and the live Story prompt. Also `TITLE_COPY_REVIEWED = false` (`sage-title.ts`) for the title samples.
2. **Playfulness poles + stems** — `POLE_COPY_REVIEWED = false` (`axis-poles.ts`): Full Profile low/high pole lines for all 16 axes including playfulness. Playfulness also has ranking items (`ranking.ts`), scenario first + second stems (`SCENARIO_DECK` + `SCENARIO_DECK_MORE`, both marked UNREVIEWED in `scenario.ts`), and `TRAIT_POLE_LINES`/mid lines in `traits.ts`.
3. **Levity copy** — the Levity category def/name (`categories.ts`), fallback band + map-quadrant lines (`category-bands.ts`, `CATEGORY_BAND_COPY_REVIEWED = false`), category concept explainer (`concept-explainers.ts`), and Explore category grounding for `cat_levity`.
4. **Category bands + category copy** — `CATEGORY_COPY_REVIEWED = false` (`categories.ts`) and `CATEGORY_BAND_COPY_REVIEWED = false` (`category-bands.ts`): offline fallback bands for every ready category and the live category line copy shown in the pinned Explore card / You fold / teaser / Circle compare.
5. **25 concept explainers** — `CONCEPT_COPY_REVIEWED = false` (`concept-explainers.ts`): 16 axis concepts + 9 category concepts (the "?" explainers on Categories, teaser, Circle compare, Sage-knows). Count updates automatically with the axis/category catalog.
6. **Dawn category grounding** — `DAWN_CATEGORY_COPY_REVIEWED = false` (`dawn-category.ts`): prompt-only meaning lines for Steadiness / Agency / Drive.
7. **Intake sweep copy** — `INTAKE_SWEEP_COPY_REVIEWED = false` (`questions/local.ts`): optional skippable IQ sweep (one item per axis) after the 9 chips.

(The `Dev only.` "copy" was not user-facing text to review — it was a stale binary 8 embedded-bundle bug, fixed in code since Aug 28 and available via binary 10 + OTA `af9b56ee`.)
